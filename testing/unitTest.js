import { pullDriverResults } from "../public/pullRaceResults";
import { requireAuth, handleValidationErrors } from "../middleware";


test('pullDriverResults correctly extracts driver results from API fetch response', () => {});


test('requireAuth allows access for authenicated users', () => {});

test('handleValidationErrors returns errors for invalid input', () => {});

test('handleValidationErrors allows valid input to proceed', () => {});